__module__ = "Extras"

__help__ = """
**Fun Commands:**

- **Pick a Winner:**
  ✧ `/pickwinner <participant1> <participant2>...` — Select a random winner from the list of participants.
 
- **Hyperlink Creation:**
  ✧ `/hyperlink <text> <link>` — Create a clickable hyperlink in the format `[text](link)`.

- **Jokes:**
  ✧ `/joke` — Get a random joke.

- **Dice Roll:**
  ✧ `/roll` — Roll a dice and get a random number between 1 and 6.

- **Flirt:**
  ✧ `/flirt` — Get a random flirtatious response.

- **Toss:**
  ✧ `/toss` — Toss a coin and get either "Heads" or "Tails."

- **Shrug Emoji:**
  ✧ `/shrug` — Get the shrug emoji (¯\\_(ツ)_/¯).

- **Blue Text:**
  ✧ `/bluetext` — Get a random "blue text" response.

- **Random Letters Generator (RLG):**
  ✧ `/rlg` — Generate a random combination of eyes, mouth, and ears to create a face.

- **Decision Making:**
  ✧ `/decide` — Get a random decision-making response (e.g., "Yes", "No", etc.).

- **Weebify Text:**
  ✧ `/weebify <text>` — Convert text into a "weebified" font.

- **Truth:**
  ✧ `/truth` — Get a random truth question.

- **Dare:**
  ✧ `/dare` — Get a random dare question.

**How to Use:**
  1. Use the commands as specified with or without arguments.
  2. Some commands require replying to a message (like `/shrug`, `/bluetext`, and `/weebify`).
  3. For commands with multiple participants (e.g., `/pickwinner`), list all participants separated by spaces.
  4. For `/hyperlink`, provide both text and link to create the hyperlink.
  5. `/roll` gives a random number 1 to 6; `/toss` gives either "Heads" or "Tails".
  6. `/truth` and `/dare` provide random questions from their respective categories.
"""